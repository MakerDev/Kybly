<Window x:Class="AutoHotkeyRemaster.UI.Views.ProfileEditor"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="Profile Editor"
        ResizeMode="NoResize"
        Height="250" Width="400">
    <Grid>
        <Grid.Background>
            <SolidColorBrush Color="{StaticResource DefaultBackgroundColor}" />
        </Grid.Background>

        <StackPanel
            HorizontalAlignment="Center"
            >
            <TextBlock 
                Margin="0 30 0 5"
                Text="Change Profile Name "
                Style="{StaticResource DefaultFontStyle}" />
            <Border
                BorderThickness="1"
                BorderBrush="White"
                Margin="0 0 0 15" />

            <TextBox
                x:Name="ProfileName"
                Style="{StaticResource DefaultStyleForTextBox}"
                HorizontalAlignment="Center" />
        </StackPanel>
    </Grid>
</Window>
